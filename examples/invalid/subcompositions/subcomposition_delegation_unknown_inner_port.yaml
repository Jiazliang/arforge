# ARForge: Invalid subcomposition fixture
# Tests validation failure for a delegation connector referencing an unknown inner component port.
subcomposition:
  name: "SubComposition_SpeedCluster"
  description: "Reusable subcomposition with an unknown inner delegation port."
  ports:
    - name: "Rp_VehicleSpeedIn"
      direction: "requires"
      interfaceRef: "If_VehicleSpeed"
  components:
    - name: "SpeedDisplay_1"
      typeRef: "SpeedDisplay"
  connectors: []
  delegationConnectors:
    - inner: "SpeedDisplay_1.Rp_Unknown"
      outer: "Rp_VehicleSpeedIn"
