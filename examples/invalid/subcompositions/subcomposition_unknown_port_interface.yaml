# ARForge: Invalid subcomposition fixture
# Tests validation failure for an unknown interface reference on a composition boundary port.
subcomposition:
  name: "SubComposition_SpeedCluster"
  description: "Reusable subcomposition with an unknown outer composition port interface."
  ports:
    - name: "Rp_VehicleSpeedIn"
      direction: "requires"
      interfaceRef: "If_Unknown"
  components:
    - name: "SpeedSensor_1"
      typeRef: "SpeedSensor"
    - name: "SpeedDisplay_1"
      typeRef: "SpeedDisplay"
  connectors:
    - from: "SpeedSensor_1.Pp_VehicleSpeed"
      to: "SpeedDisplay_1.Rp_VehicleSpeed"
