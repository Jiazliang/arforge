# ARForge: Profile demo fixture
# Intentionally contains naming and hygiene violations for the sample profiles.
autosar:
  version: "4.2"
  rootPackage: "PROFILE_DEMO"

inputs:
  baseTypes: "../../invalid/support/base_types.yaml"
  implementationDataTypes: "../../invalid/support/implementation_types.yaml"
  applicationDataTypes: "../../invalid/support/application_types.yaml"
  units:
    - "../../invalid/support/units.yaml"
  compuMethods:
    - "../../invalid/support/compu_methods.yaml"
  interfaces:
    - "interfaces/*.yaml"
  swcs:
    - "swcs/*.yaml"
  system: "system.yaml"
