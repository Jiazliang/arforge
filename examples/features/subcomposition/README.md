# Subcomposition Example

This project focuses on reusable composition types.

It highlights:

- a subcomposition with composition boundary ports
- inner component prototypes and internal assembly connectors
- delegation connectors that map boundary ports to inner ports
- a top-level system that instantiates the subcomposition beside a standalone atomic SWC

Entry point: `examples/features/subcomposition/autosar.project.yaml`
